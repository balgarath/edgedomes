#!/bin/bash
ruby run.rb
git add .
git commit -a -m 'generating static files' || 0
git push && \
git checkout gh-pages && \

git checkout master -- public/* && \
rm *.html
rm -rf assets
cp -Rf public/* .
git commit -m "syncing gh-pages branch to master" && \
git push && \
git checkout master

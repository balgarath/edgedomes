#!/bin/bash

git push && \
git checkout gh-pages && \

git checkout master -- public/* && \
cp -Rf public/* .
rm -rf public
git commit -m "syncing gh-pages branch to master" && \
git push && \
git checkout master
